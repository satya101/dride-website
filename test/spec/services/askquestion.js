'use strict';

describe('Service: askQuestion', function () {

  // load the service's module
  beforeEach(module('drideApp'));

  // instantiate service
  var askQuestion;
  beforeEach(inject(function (_askQuestion_) {
    askQuestion = _askQuestion_;
  }));

  // it('should do something', function () {
  //   expect(!!askQuestion).toBe(true);
  // });

});
