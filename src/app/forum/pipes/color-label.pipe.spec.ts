import { ColorLabelPipe } from './color-label.pipe';

describe('ColorLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
